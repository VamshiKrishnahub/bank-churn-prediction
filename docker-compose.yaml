#version: "3.9"
#
#services:
#  # ------------------------------
#  # PostgreSQL database
#  # ------------------------------
#  db:
#    image: postgres:15
#    container_name: defence_db
#    ports:
#      - "5432:5432"
#    environment:
#      POSTGRES_USER: admin
#      POSTGRES_PASSWORD: admin
#      POSTGRES_DB: defence_db
#    volumes:
#      - db_data:/var/lib/postgresql/data
#
#  # ------------------------------
#  # FastAPI backend
#  # ------------------------------
#  fastapi:
#    build: ./fastapi_app
#    container_name: defence_api
#    depends_on:
#      - db
#    ports:
#      - "8000:8000"
#    environment:
#      DATABASE_URL: postgresql+psycopg2://admin:admin@db:5432/defence_db
#      PYTHONPATH: /app:/workspace
#    volumes:
#      - ./fastapi_app:/app
#      - ./database:/workspace/database
#      - ./model:/workspace/model
#    working_dir: /app
#    command: ["uvicorn", "main_api:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
#
#  # ------------------------------
#  # Streamlit frontend
#  # ------------------------------
#  webapp:
#    build: ./webapp
#    container_name: defence_streamlit
#    depends_on:
#      - fastapi
#    ports:
#      - "8501:8501"
#    environment:
#      FASTAPI_URL: http://fastapi:8000
#    volumes:
#      - ./webapp:/app
#    command: ["streamlit", "run", "main.py", "--server.address=0.0.0.0"]
#
#  # ------------------------------
#  # Airflow
#  # ------------------------------
#  airflow:
#    image: apache/airflow:2.7.3
#    container_name: defence_airflow
#    depends_on:
#      - fastapi
#      - db
#    environment:
#      AIRFLOW__CORE__EXECUTOR: SequentialExecutor
#      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
#      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://admin:admin@db:5432/defence_db
#
#      # Custom ENV for DAG code
#      RAW_DATA_DIR: /opt/airflow/Data/raw
#      GOOD_DATA_DIR: /opt/airflow/Data/good_data
#      FASTAPI_URL: http://fastapi:8000/predict
#      DATABASE_URL: postgresql+psycopg2://admin:admin@db:5432/defence_db
#      PYTHONPATH: /opt/airflow:/opt/airflow/dags:/opt/airflow/database
#    volumes:
#      - ./airflow/dags:/opt/airflow/dags
#      - ./Data:/opt/airflow/Data
#      - ./database:/opt/airflow/database
#    ports:
#      - "8080:8080"
#    command: ["bash", "-c", "airflow db init && airflow users create --username admin --password admin --role Admin --firstname Max --lastname Fry --email admin@example.com && airflow webserver & airflow scheduler"]
#
#volumes:
#  db_data:

#version: '3.9'
#
#services:
#  # ================================
#  # PostgreSQL database
#  # ================================
#  db:
#    image: postgres:15
#    container_name: defence_db
#    ports:
#      - "5432:5432"
#    environment:
#      POSTGRES_USER: admin
#      POSTGRES_PASSWORD: admin
#      POSTGRES_DB: defence_db
#    volumes:
#      - db_data:/var/lib/postgresql/data
#    networks:
#      - defence_network
#
#  # ================================
#  # FastAPI Backend
#  # ================================
#  fastapi:
#    build: ./fastapi_app
#    container_name: defence_api
#    depends_on:
#      - db
#    ports:
#      - "8000:8000"
#    environment:
#      DATABASE_URL: postgresql+psycopg2://admin:admin@db:5432/defence_db
#      PYTHONPATH: /app:/workspace
#      FASTAPI_PUBLIC_URL: http://localhost:8000
#    volumes:
#      - ./fastapi_app:/app
#      - ./database:/workspace/database
#      - ./model:/workspace/model
#      - ./Data:/opt/airflow/Data
#    working_dir: /app
#    command: ["uvicorn", "main_api:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
#    networks:
#      - defence_network
#
#  # ================================
#  # Streamlit Webapp
#  # ================================
#  webapp:
#    build: ./webapp
#    container_name: defence_streamlit
#    depends_on:
#      - fastapi
#    ports:
#      - "8501:8501"
#    environment:
#      FASTAPI_URL: http://fastapi:8000
#    volumes:
#      - ./webapp:/app
#    command: ["streamlit", "run", "main.py", "--server.address=0.0.0.0"]
#    networks:
#      - defence_network
#
#  # ================================
#  # Airflow (Custom Image)
#  # ================================
#  airflow:
#    build: ./airflow
#    container_name: defence_airflow
#    depends_on:
#      - fastapi
#      - db
#    environment:
#      AIRFLOW__CORE__EXECUTOR: SequentialExecutor
#      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
#      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://admin:admin@db:5432/defence_db
#
#      RAW_DATA_DIR: /opt/airflow/Data/raw
#      GOOD_DATA_DIR: /opt/airflow/Data/good_data
#      FASTAPI_URL: http://fastapi:8000/predict
#      DATABASE_URL: postgresql+psycopg2://admin:admin@db:5432/defence_db
#
#      FASTAPI_PUBLIC_URL: http://localhost:8000
#      TEAMS_WEBHOOK: ${TEAMS_WEBHOOK}
#
#      PYTHONPATH: /opt/airflow:/opt/airflow/dags:/opt/airflow/database
#    volumes:
#      - ./airflow/dags:/opt/airflow/dags
#      - ./Data:/opt/airflow/Data
#      - ./database:/opt/airflow/database
#    ports:
#      - "8080:8080"
#    command: ["bash", "-c",
#      "airflow db init &&
#       airflow users create --username admin --password admin --role Admin --firstname Max --lastname Fry --email admin@example.com &&
#       airflow webserver & airflow scheduler"
#    ]
#    networks:
#      - defence_network
#
#  # ================================
#  # Grafana (Monitoring Dashboards)
#  # ================================
#  grafana:
#    image: grafana/grafana:latest
#    container_name: defence_grafana
#    depends_on:
#      - db
#    ports:
#      - "3000:3000"
#    environment:
#      # Admin credentials
#      - GF_SECURITY_ADMIN_USER=admin
#      - GF_SECURITY_ADMIN_PASSWORD=admin
#
#      # Install plugins
#      - GF_INSTALL_PLUGINS=grafana-clock-panel,grafana-piechart-panel
#
#      # Disable login form (optional - for easier access)
#      - GF_AUTH_ANONYMOUS_ENABLED=false
#
#      # Allow embedding (optional)
#      - GF_SECURITY_ALLOW_EMBEDDING=true
#
#      # Server settings
#      - GF_SERVER_ROOT_URL=http://localhost:3000
#      - GF_SERVER_SERVE_FROM_SUB_PATH=false
#    volumes:
#      # Persistent storage
#      - grafana_data:/var/lib/grafana
#
#      # Auto-provisioning dashboards and datasources
#      - ./grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards
#      - ./grafana/provisioning/datasources:/etc/grafana/provisioning/datasources
#
#      # Dashboard JSON files
#      - ./grafana/dashboards:/var/lib/grafana/dashboards
#    networks:
#      - defence_network
#    restart: unless-stopped
#
## ================================
## NETWORKS
## ================================
#networks:
#  defence_network:
#    driver: bridge
#
## ================================
## VOLUMES
## ================================
#volumes:
#  db_data:
#  grafana_data:

version: '3.9'

services:
  # ================================
  # PostgreSQL database
  # ================================
  db:
    image: postgres:15
    container_name: defence_db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: defence_db
    volumes:
      - db_data:/var/lib/postgresql/data
    networks:
      - defence_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d defence_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ================================
  # FastAPI Backend
  # ================================
  fastapi:
    build: ./fastapi_app
    container_name: defence_api
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+psycopg2://admin:admin@db:5432/defence_db
      PYTHONPATH: /app:/workspace
      FASTAPI_PUBLIC_URL: http://localhost:8000
    volumes:
      - ./fastapi_app:/app
      - ./database:/workspace/database
      - ./model:/workspace/model
      - ./Data:/opt/airflow/Data
    working_dir: /app
    command: ["uvicorn", "main_api:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    networks:
      - defence_network

  # ================================
  # Streamlit Webapp
  # ================================
  webapp:
    build: ./webapp
    container_name: defence_streamlit
    depends_on:
      - fastapi
    ports:
      - "8501:8501"
    environment:
      FASTAPI_URL: http://fastapi:8000
    volumes:
      - ./webapp:/app
    command: ["streamlit", "run", "main.py", "--server.address=0.0.0.0"]
    networks:
      - defence_network

  # ================================
  # Airflow (Custom Image)
  # ================================
  airflow:
    build: ./airflow
    container_name: defence_airflow
    depends_on:
      db:
        condition: service_healthy
      fastapi:
        condition: service_started
    environment:
      AIRFLOW__CORE__EXECUTOR: SequentialExecutor
      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://admin:admin@db:5432/defence_db

      RAW_DATA_DIR: /opt/airflow/Data/raw
      GOOD_DATA_DIR: /opt/airflow/Data/good_data
      FASTAPI_URL: http://fastapi:8000/predict
      DATABASE_URL: postgresql+psycopg2://admin:admin@db:5432/defence_db

      FASTAPI_PUBLIC_URL: http://localhost:8000
      TEAMS_WEBHOOK: ${TEAMS_WEBHOOK}

      PYTHONPATH: /opt/airflow:/opt/airflow/dags:/opt/airflow/database
    volumes:
      - ./airflow/dags:/opt/airflow/dags
      - ./Data:/opt/airflow/Data
      - ./database:/opt/airflow/database
    ports:
      - "8080:8080"
    command: ["bash", "-c",
      "airflow db init &&
       airflow users create --username admin --password admin --role Admin --firstname Max --lastname Fry --email admin@example.com || true &&
       airflow webserver & airflow scheduler"
    ]
    networks:
      - defence_network

  # ================================
  # Grafana (Monitoring Dashboards)
  # ================================
  grafana:
    image: grafana/grafana:latest
    container_name: defence_grafana
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "3000:3000"
    environment:
      # Admin credentials
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin

      # Install plugins
      - GF_INSTALL_PLUGINS=grafana-clock-panel,grafana-piechart-panel

      # Server settings
      - GF_SERVER_ROOT_URL=http://localhost:3000

      # Log level for debugging
      - GF_LOG_LEVEL=info
    volumes:
      # Persistent storage
      - grafana_data:/var/lib/grafana

      # Auto-provisioning dashboards and datasources
      - ./grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards
      - ./grafana/provisioning/datasources:/etc/grafana/provisioning/datasources

      # Dashboard JSON files
      - ./grafana/dashboards:/var/lib/grafana/dashboards
    networks:
      - defence_network
    restart: unless-stopped

# ================================
# NETWORKS
# ================================
networks:
  defence_network:
    driver: bridge
    name: defence_network

# ================================
# VOLUMES
# ================================
volumes:
  db_data:
  grafana_data: